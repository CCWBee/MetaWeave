# MetaWeave

A one-page tool for tagging documents with YAML metadata comments. Paste the content, set the fields, and it threads the metadata block in where it belongs. This is the v2 pass.

## Use

Open `index.html`.
